# Annisweb Portfolio (v2)

Modern portfolio — React 19, TypeScript (strict), TanStack Router / Query / Table, Tailwind CSS 4, Vite.

## Run

```
cd v2
npm install   # first time only
npm run dev
```

→ http://localhost:5173/
