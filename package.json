{
  "name": "annisweb",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix v2",
    "start": "npm run start --prefix v2",
    "build": "npm run build --prefix v2",
    "preview": "npm run preview --prefix v2"
  }
}
